package re.tsuku.confikure.model;

/**
 * editor kinds supported by the default config gui.
 */
public enum EditorType {
    AUTO, BOOLEAN, NUMBER, TEXT, MULTILINE_TEXT, DROPDOWN, MODE, COLOR, KEYBIND, BUTTON, DRAGGABLE_LIST, INFO, CUSTOM
}
